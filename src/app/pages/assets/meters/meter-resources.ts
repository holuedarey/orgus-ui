export enum MeterResources {
    CreateMeter = 'meter:create-meter',
    UpdateMeter = 'meter:update-meter',
    DeleteMeter = 'meter:delete-meter',
    ViewClientColumn = 'meter:view-client-column',
}